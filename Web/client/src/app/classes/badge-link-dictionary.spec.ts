import { BadgeLinkDictionary } from './badge-link-dictionary';

describe('BadgeLinkDictionary', () => {
  it('should create an instance', () => {
    expect(new BadgeLinkDictionary()).toBeTruthy();
  });
});
