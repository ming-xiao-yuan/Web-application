import { LobbyPlayerInterface } from './lobby-player-interface';

describe('LobbyPlayerInterface', () => {
  it('should create an instance', () => {
    expect(new LobbyPlayerInterface()).toBeTruthy();
  });
});
