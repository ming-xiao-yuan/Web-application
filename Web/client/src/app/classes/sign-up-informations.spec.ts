import { SignUpInformations } from './sign-up-informations';

describe('SignUpInformations', () => {
  it('should create an instance', () => {
    expect(new SignUpInformations()).toBeTruthy();
  });
});
