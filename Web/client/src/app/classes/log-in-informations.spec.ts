import { LogInInformations } from './log-in-informations';

describe('LogInInformations', () => {
  it('should create an instance', () => {
    expect(new LogInInformations()).toBeTruthy();
  });
});
